# Rust / Cargo build output — never committed.
/target
**/target/
/dist

# Packaging / installer artifacts (built in CI, attached to releases).
*.msi
*.deb
*.pdb

# Editor / back-up cruft
**/*.rs.bk

# Local working docs (roadmap / scratch) — not shipped.
FEATURES.md
draft-tasks.txt

# Local LLM model files — never committed (multi-GB; user-supplied).
/models/
*.gguf
*.crdownload
